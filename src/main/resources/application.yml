server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password:
    url: jdbc:h2:mem:demo
  h2:
    console:
      enabled: true
      path: /h2
  jpa:
    generate-ddl: false
    open-in-view: false
    hibernate:
      ddl-auto: update